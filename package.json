{
  "name": "identity-circle",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Maya Wright",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd backend && npx nodemon server.js\" \"cd frontend && npx expo start\"",
    "dev:ios": "concurrently \"cd backend && npx nodemon server.js\" \"cd frontend && npx expo start --ios\"",
    "frontend": "cd frontend && npx expo start --ios",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
